# docker-setup
Install Docker Engine on Ubuntu or Debian from Docker's own APT repository, interactive or unattended. Verifies the signing key fingerprint, sets log rotation so containers never fill the disk, grants a user sudo-free access, runs a test container. Also --status and --update.
